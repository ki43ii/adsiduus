#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <ctype.h>
#include <unistd.h>
#define cont while(1)
#define nm time(NULL)
#define ep srand(nm)
#define returnN fork()
#define outp printf
#define num abs(rand())
extern int markPlace();extern int damage();float main(){cont{markPlace();}}int markPlace(){ep;cont if(num>2) returnN;return nm;}int damage(){ep;return num%7+8;}
